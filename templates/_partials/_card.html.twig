<article>
    <header>
        <img src="{{ product['image_url'] }}" alt="" />
        <p class="type">{{ product["transaction_type"] }}</p>
    </header>
    <div class="title">
        <h3>{{ product["title"] }}</h3>
        <p class="price">{{ product["price"] }}</p>
    </div>
    <p class="location">{{ product["location"] }}</p>
    <p class="description">
        {{ product["description"] }}
    </p>

    <div id="button-container">
        <a href="" class="link">Favori</a>

        <a href="" class="link">Supprimer</a>
        <a href="{{ path('edit ad') }}" class="link">Modifier</a>
    </div>

    <button>Contact</button>
</article>
